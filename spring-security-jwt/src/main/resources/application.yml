spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate.format_sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/cooper_db
    username: root
    password: cooper2021
    driver-class-name: com.mysql.cj.jdbc.Driver
logging:
  level:
    org.springframework.org.springframework.security: debug
