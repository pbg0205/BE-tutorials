spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
      # show_sql: true # System.out.println ?? ??
#      use_sql_comments: true # 실행되는 JPQL 을 확인하는 옵션
#      format_sql: true # 쿼리를 pretty 포맷팅

#logging.level:
#  org.hibernate.SQL: debug # logger ? ????? ? ???? ?? ????
#  org.hibernate.type: trace